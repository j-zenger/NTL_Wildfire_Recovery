---
title: "Energy_viz"
author: "Josie Zenger"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}

calenergy <- read.csv("~/Local/Dissertation/calenergy.csv")

calenergy <- calenergy %>%
  filter(CountyName == "BUTTE")

# Line plot of enrgy usage over time by different kinds of energy 
# add vertical line to indicate 2018 Camp Fire

library(ggplot2)
ggplot(calenergy, aes(x = Year, y = GWh, color = Sector)) +
  geom_line() +
  labs(title = "Energy Usage Over Time in Butte County",
       x = "Date",
       y = "Energy Usage (kWh)") +
  geom_vline(xintercept = 2019, linetype = "dashed", color = "red") +
  theme_minimal()


```